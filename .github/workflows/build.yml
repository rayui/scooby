name: ci
on:
  push:
    branches:
      - "main"
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout the code
        uses: actions/checkout@v3
      - name: Cache Vagrant boxes
        uses: actions/cache@v2
        with:
          path: ~/.vagrant.d/boxes
          key: ${{ runner.os }}-vagrant-${{ hashFiles('Vagrantfile') }}
          restore-keys: |
            ${{ runner.os }}-vagrant-
      - name: Build
        run: ./scripts/build.sh
        env:
          LC_ALL: en_US.UTF-8
          LC_IMAGE_HREF: ${{vars.LC_IMAGE_HREF}}
          LC_IMAGE_SHA: ${{vars.LC_IMAGE_SHA}}
          LC_SSH_AUTH_KEY: ${{secrets.SSH_AUTH_KEY}}
          LC_PACKER_GITHUB_API_TOKEN: ${{secrets.PACKER_GITHUB_API_TOKEN}}
          LC_EXTERNAL_IP: ${{secrets.LC_EXTERNAL_IP}}
          LC_EXTERNAL_NM: ${{secrets.LC_EXTERNAL_NM}}
          LC_EXTERNAL_GW: ${{secrets.LC_EXTERNAL_GW}}
          LC_EXTERNAL_DOMAIN: ${{secrets.LC_EXTERNAL_DOMAIN}}
          LC_EXTERNAL_DNS: ${{secrets.LC_EXTERNAL_DNS}}
          LC_EXTERNAL_DEVICE: ${{secrets.LC_EXTERNAL_DEVICE}}
          LC_LOCAL_DNS: ${{secrets.LC_LOCAL_DNS}}
          LC_INTERNAL_DEVICE: ${{secrets.LC_INTERNAL_DEVICE}}
          LC_INTERNAL_IP: ${{secrets.LC_INTERNAL_IP}}
          LC_INTERNAL_NM: ${{secrets.LC_INTERNAL_NM}}
          LC_INTERNAL_DOMAIN: ${{secrets.LC_INTERNAL_DOMAIN}}
          LC_DEFAULT_USER: ${{secrets.LC_DEFAULT_USER}}
